### Installation

Firstly clone this repo:

```git clone https://github.com/us/english-please.git```

then install with 

```./install.sh```

that'll add job to `cronjob` and is run twice a day.

Then edit `run_script.sh` according to your own git repo.